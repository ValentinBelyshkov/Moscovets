# Навигация к модулям - Тест

## Изменения, внесенные для решения задачи

### 1. Исправлена навигация в PatientCard.js
- Изменен метод `navigateToModule` с использования `window.location.hash` на React Router `navigate()`
- Добавлен импорт `useNavigate` из react-router-dom
- Навигация теперь передает данные пациента в состоянии

### 2. Создан хук usePatientNavigation.js
- Автоматически обрабатывает данные пациента при навигации
- Обновляет контекст данных с информацией о пациенте
- Очищает состояние навигации после использования

### 3. Обновлены все модули для поддержки навигации
- BiometryModule.js - добавлена поддержка usePatientNavigation
- PhotometryModuleRefactored.js - добавлена поддержка навигации
- CephalometryModuleRefactored.js - добавлена поддержка навигации  
- ModelingModule.js - добавлена поддержка навигации
- CTModule.js - добавлена поддержка навигации

## Функциональность

Теперь при нажатии на любую из 4 кнопок внизу страницы пациента:

1. **Фотометрия** - переход к `/photometry` с данными пациента
2. **Цефалометрия** - переход к `/cephalometry` с данными пациента
3. **Биометрия** - переход к `/biometry` с данными пациента
4. **3D Модели** - переход к `/modeling` с данными пациента

Каждый модуль получает данные пациента и может работать с ними через контекст.

## Кнопки в PatientCard.js (строки 617-640)

```javascript
<button onClick={() => navigateToModule('photometry')}>
  <span>📷</span> Фотометрия
</button>
<button onClick={() => navigateToModule('cephalometry')}>
  <span>🦴</span> Цефалометрия
</button>
<button onClick={() => navigateToModule('biometry')}>
  <span>📐</span> Биометрия
</button>
<button onClick={() => navigateToModule('modeling')}>
  <span>🖥️</span> 3D Модели
</button>
```

## Маршруты в App.js (строки 110-114)

Все маршруты настроены правильно:
- `/photometry` - PhotometryModule
- `/cephalometry` - CephalometryModule  
- `/biometry` - BiometryModule
- `/modeling` - ModelingModule
- `/ct` - CTModule