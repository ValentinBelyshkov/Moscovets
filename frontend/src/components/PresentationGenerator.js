import PresentationGeneratorRefactored from './presentation/PresentationGeneratorRefactored';

const PresentationGenerator = (props) => {
  return <PresentationGeneratorRefactored {...props} />;
};

export default PresentationGenerator;
